import SwiftUI

struct ContentView: View {
    var body: some View {
        HStack {
            Text("Leading")
            Spacer()
            Text("Trailing")
        }
        .padding()
    }
}
